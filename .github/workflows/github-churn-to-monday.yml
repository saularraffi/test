name: Send GH PR metrics to Monday Board

on:
  push:
    branches:
      - main

jobs:
  call-gh-pr-metrics-to-monday:
    uses: saularraffi/test/.github/workflows/github-churn-to-monday.yml@main
    with:
      repo-owner: ${{ github.repository_owner }}
      repo-name: ${{ github.event.repository.name }}
    secrets: inherit
# on:
#   push:
#     branches:
#       - main

# jobs:
#   call-gh-pr-metrics-to-monday:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Call GH PR Metrics to Monday
#         uses: saularraffi/test/.github/workflows/github-churn-to-monday.yml@main
#         with:
#           repo-owner: saularraffi
#           repo-name: test
#         secrets: inherit

